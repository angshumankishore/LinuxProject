# 🛡️ Simple Role-Based Access Control (RBAC) in C

This project is a **simple console-based C program** that demonstrates how **Role-Based Access Control (RBAC)** works.  
It shows how different users (Viewer, Editor, Admin) can access different levels of functionality based on their assigned roles.

---

## 🚀 Features

- 👤 **User Authentication** – login system with username & password  
- 🔐 **Role-based Permissions**
  - **Viewer**: Can only *view* records  
  - **Editor**: Can *view* and *edit* records  
  - **Admin**: Has full access (*view*, *edit*, *delete*)  
- 🧱 **Predefined Users**
  - `alice` (Viewer)
  - `bob` (Editor)
  - `admin` (Admin)
- 🖥️ **Menu-driven interface** for simplicity  

---

## 📂 Project Structure

